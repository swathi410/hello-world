name: Hello World Workflow

on:
  push:
    branches:
      - main   

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print Hello World iam learner
        run: echo "Hello World from GitHub Actions 🚀"
